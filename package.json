{
  "name": "tabs-view",
  "private": true,
  "version": "0.0.1",
  "description": "Simple TabsView application",
  "repository": "https://github.com/angular/angular-seed",
  "devDependencies": {
    "http-server": "^0.6.1",
    "shelljs": "^0.2.6"
  },
  "scripts": {
    "prestart": "npm install",
    "start": "http-server -a localhost -p 8000 -c-1"
  }
}
